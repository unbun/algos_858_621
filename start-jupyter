#!/usr/bin/env bash
set -e

cd "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")"

if [ ! -f .venv/bin/activate ]; then
    echo "No .venv found in $(pwd). Run 'uv sync' first." >&2
    exit 1
fi

source .venv/bin/activate
exec jupyter lab "$@"
